# OpenGL Template (C++)
![Gif of opengl template](gif.gif)

*giphy downscaled fps and quality*

## Compile & Run (All in one)
```
make && cd dist/ && ./opengl-template
```

### Keybinds
- Movement: WASD
- Look Around: Mouse

## Can I do 2D with this?
Yes, simply just remove the camera (model, view, projection), change the vertices and indices and remove the glm::vec3 cubePositions[].
# opengl-template
